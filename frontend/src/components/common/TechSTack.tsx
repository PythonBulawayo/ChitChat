import React from "react";


const TechSTack = ({ title }: { title: string }) => {
	return <div className="bg-gray-300 text-subheading-100 rounded-xl p-1 text-sm">{title}</div>;
};

export default TechSTack;
